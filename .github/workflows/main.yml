name: Deploy to Ubuntu Server with SSH Key

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'

      - name: Install html-minifier
        run: npm install -g html-minifier

      # ✅ 개인키는 ssh-agent로 로드
      - name: Start ssh-agent and add key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: |
            ${{ secrets.SSH_PRIVATE_KEY }}

      # ✅ known_hosts 추가 (IP/호스트명 확인)
      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -T 10 ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Obfuscate HTML files
        run: |
          set -x
          for file in $(find . -name '*.html'); do
            html-minifier --config-file html-minifier.json -o "$file" "$file"
          done

      - name: Rsync to server
        run: |
          rsync -avz -e "ssh -o StrictHostKeyChecking=yes" ./ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:${{ secrets.TARGET_DIRECTORY }}
